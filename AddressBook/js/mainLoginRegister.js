require(["lib/jquery-latest",  "lib/jquery.validate", "lib/form-validation"], function(_1,_2, _3 ) {
/*     var serviceModule = new service.AddressBookService();
     $('#login_form').submit(function(event) {
    	 //loginManager.serialize();    	 
    	 var loginValues = $('#login_form').serialize();
    	 console.log('here');
    	 serviceModule.login(loginValues);
     });*/
});