require(["lib/jquery-latest",  "lib/jquery.validate", "lib/form-validation"], function(_1,_2, _3 ) {
	/**
	 * This module is responsible for wiring dependencies needed by login and registration pages.
	 */
});